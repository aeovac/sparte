import { Sparte } from "./sparte";

// Nous utiliserons des fonctions (Spart()) pour déclarer les clients
// Economie de mémoire (par rapport aux classes) + Adaptation à createBot plus simple

// les sb sont stockés qq part?
// Manager? 
// On verra comment on gère les process
// Don't worry
// 1 process per user?
// Comment tu faisais?
// toutes les class sur un process
// Oui voilào
export const sparte = Sparte('token');